.MODEL SMALL
.STACK 100H
.DATA

MSG DB 0AH,0DH, 'HELLO WORLD$'

.CODE

MAIN PROC
  MOV AX,@DATA
  MOV DS,AX
  
  MOV CX,5
  
  PRINT_LOOP:
  LEA DX,MSG.
  MOV AH,9
  INT 21H
  
  JG PRINT_LOOP
  
  EXIT:
  MOV AH,4CH
  INT 21H
  
  MAIN ENDP
END MAIN
